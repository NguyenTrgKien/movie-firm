import style from './MovieGrid.module.scss';
import clsx from 'clsx';

function MovieGrid() {
    return (  
        <section className={clsx(style['movie-grid'])}>

        </section>
    );
}

export default MovieGrid;